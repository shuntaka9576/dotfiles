git:
  parseEmoji: true
  autoFetch: false

gui:
  showIcons: true
  screenMode: "normal"
  sidePanelWidth: 0.5

os:
  editPreset: "nvim"

customCommands:
  - key: "<c-g>"
    context: "files"
    description: "AI commit message (codex gpt-5.5)"
    command: "bash ~/dotfiles/home-manager/programs/lazygit/ai-commit.sh"
    loadingText: "Generating commit message with codex..."
    output: log
